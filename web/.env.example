# Poly Meridian web — local dev environment

# Public — exposed to browser. Point at the agent's API host.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-only — used by next.config.js rewrites if you proxy through Next.
AGENT_API_URL=http://localhost:8000
